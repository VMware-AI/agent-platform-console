# GraphQL endpoint of agent-platform-backend. For local dev, copy this to
# .env.local (gitignored) and point it at your running backend:
#   VITE_GRAPHQL_ENDPOINT=http://localhost:8080/query
# Leave unset to default to the same-origin /query.
VITE_GRAPHQL_ENDPOINT=http://localhost:8080/query
