# GraphQL endpoint of agent-platform-backend.
# For local dev, copy this file to .env.local (gitignored) and point it at your
# running backend. Leave unset to default to the same-origin /query.
#   VITE_GRAPHQL_ENDPOINT=http://localhost:8080/query
VITE_GRAPHQL_ENDPOINT=http://localhost:8080/query
